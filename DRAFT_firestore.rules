rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Relaxed rules to diagnose if rules are even active
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
